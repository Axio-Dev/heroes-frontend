# Heroes APP

## Levantar desarrollo

1. Clonar repositorio
2. Editar el archivo `.env` con las varibales de entorno basado en el archivo `env.template`
3. Ejecutar `yarn install`
4. Ejecutar `yarn run dev`
